---
receivers:
  kubeletstats/microshift:
    collection_interval: 20s
    auth_type: "none"
    endpoint: "https://${env:K8S_HOST_IP}:10250"
    insecure_skip_verify: true

# TODO: add filelog & kube event receivers
